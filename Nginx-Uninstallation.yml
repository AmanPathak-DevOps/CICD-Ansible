--- # Uninstallig Nginx
- hosts: nginx_server
  user: ubuntu
  become: yes
  connection: ssh
  gather_facts: yes

  tasks:

    - name: "Uninstall NginX"
      apt:
        name: nginx
        state: absent
        autoremove: yes

    - name: stopping nginx
      service:
        name: nginx
        state: stopped